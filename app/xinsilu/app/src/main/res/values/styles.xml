<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PopupAnimation">
        <item name="android:windowEnterAnimation">@anim/menu_bottombar_in</item>
        <item name="android:windowExitAnimation">@anim/menu_bottombar_out</item>
    </style>
    <style name="customer_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>


    <!-- 自定义宽高 -->
    <style name="myStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <!-- 个人中心四个布局 -->
    <style name="info_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    <!--垂直比重布局-->
    <style name="info_v_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
    </style>
    <!-- 基本内间距为 10dp
         距离左边和右边都为10dp，及左右各20dp
         改的话可以在这里改-->
    <style name="BasicParam" parent="@style/info_v_style"></style>

    <!-- 基本内间距为左右为20dp
         上下为10dp
         改的话可以在这里改-->
    <style name="BasicParamNoMargin_v" parent="@style/info_v_style">
        <item name="android:paddingRight">@dimen/user_padding_without_margin</item>
        <item name="android:paddingLeft">@dimen/user_padding_without_margin</item>
        <item name="android:paddingTop">@dimen/user_padding</item>
        <item name="android:paddingBottom">@dimen/user_padding</item>
    </style>

    <!-- 基本内间距为左右为20dp
         上下为10dp
         改的话可以在这里改-->
    <style name="BasicParamNoMargin_h" parent="@style/info_style">
        <item name="android:gravity">center</item>
    </style>

    <style name="Basic_params">
        <item name="android:paddingRight">@dimen/user_padding_without_margin</item>
        <item name="android:paddingLeft">@dimen/user_padding_without_margin</item>
        <item name="android:paddingTop">@dimen/user_padding</item>
        <item name="android:paddingBottom">@dimen/user_padding</item>
    </style>

    <style name="hor_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="hor_style_h">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="hor_style_padding" parent="hor_style">
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingRight">20dp</item>
        <item name="android:paddingBottom">14dp</item>

    </style>

    <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/food_ratingbar_full</item>
        <item name="android:minHeight">24dp</item>
        <item name="android:maxHeight">24dp</item>
        <item name="android:isIndicator">true</item>
    </style>

    <!--此部分为viewpagerindicator的style-->
    <style name="Widget" />

    <style name="TextAppearance.TabPageIndicator" parent="Widget">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/vpi__dark_theme</item>
    </style>

    <style name="Widget.TabPageIndicator" parent="Widget">
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:background">@drawable/vpi__tab_indicator</item>
        <item name="android:paddingLeft">22dip</item>
        <item name="android:paddingRight">22dip</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAppearance">@style/TextAppearance.TabPageIndicator</item>
        <item name="android:textSize">16sp</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
